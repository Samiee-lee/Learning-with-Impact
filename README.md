# Learning With Impact by Evolution Academy

Phase 3a — a live, branded dashboard connected to Supabase.

## Deploy
This is a standard Next.js app. Deploy on Vercel by importing the GitHub repo
(framework auto-detects as Next.js). No configuration required — the Supabase
connection is built in.

## Local run (optional, not required)
    npm install
    npm run dev
